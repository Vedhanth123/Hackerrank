
array = [5,3,2,1,0]

# 1) divide the array into two parts

left = array[:len(array)//2]
right = array[(len(array)//2):]

print(left)
print(right)

# 2) division should stop when the len(array) becomes 1

# 3) merge the array

i = 0
j = len(right) // 2
k = 0

while(i < len(left) )



# O(n^2)


# 2) find the smallest possible value
    
# for i in range():
# O(n)

# return

# O(n^2) + O(n)

# O(n) < O(n.logn) < O(n^2)